<!-- toast -->   
<view class="c-toast">
    <view class="_content" hidden="{{!show}}">
        <icon type="{{iconType}}" size="40" class="__icon" hidden="{{!showIcon}}"/>
        <text>{{msg}}</text>
    </view>    
</view>